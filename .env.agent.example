# Copy to .env before running: cp .env.agent.example .env
# Generate a token from the central instance's Settings -> Agents -> Generate,
# or `openssl rand -hex 32`. Must be at least 16 characters.

DOCKWATCH_AGENT_TOKEN=change-me-openssl-rand-hex-32
